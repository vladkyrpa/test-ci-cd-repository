{
  "name": "test-ci-cd-repository",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "release:bootstrap": "dotenv -e .env.release -- release-please bootstrap --token=$GITHUB_TOKEN --repo-url=$REPO --release-type=node",
    "release:pr1": "dotenv -e .env.release -- sh -c 'release-please release-pr --token=$GITHUB_TOKEN --repo-url=$REPO'",
    "release:pr": "dotenv -e .env.release -- release-please release-pr --token=$GITHUB_TOKEN --repo-url=$REPO",
    "release:github": "dotenv -e .env.release -- sh -c 'release-please github-release --token=$GITHUB_TOKEN --repo-url=$REPO'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vladkyrpa/test-ci-cd-repository.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/vladkyrpa/test-ci-cd-repository/issues"
  },
  "homepage": "https://github.com/vladkyrpa/test-ci-cd-repository#readme",
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.6",
    "@semantic-release/npm": "^13.1.4",
    "@semantic-release/release-notes-generator": "^14.1.0",
    "release-please": "^15.13.0",
    "semantic-release": "^25.0.3"
  }
}
